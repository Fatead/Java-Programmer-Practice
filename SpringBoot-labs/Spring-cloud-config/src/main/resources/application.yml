server:
  port: 3344

spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri:           # github上 git仓库的名字
          search-paths:  # 搜索目录
            - springcloud-config
      label: master
